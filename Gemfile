source 'https://rubygems.org'

gem 'rake'                        # Build tool
gem 'watir'

gem 'irb'

gem 'watir-scroll'                # Easy element scrolling
gem 'page-object'

gem 'require_all'                 # Easily load large directories

# Indirect dependencies (libraries that are required by a direct dependency but that we have to call out directly for various reasons)
gem 'selenium-webdriver', '>= 3.6.0' # Need at least this version in order to correctly work with SeleniumGrid 3.5+ with Firefox
# rubocop:enable Style/AlignParameters

#gem 'activerecord'
#gem 'mysql2'                      #mysql2 gem
gem 'cucumber'                    #cucumber gem

#gem 'cuke_cataloger'              #ids test cases in feature files

gem 'waitutil'                    #Gem that waits until condition is met

gem 'rspec-expectations'          # Assertion library that provides #expect and various handy matchers

gem 'parallel_tests', group: [:development, :test]              #gem to run cucumber tests in paralell

gem 'rest-client'                 # Used for making REST calls

gem 'webdrivers'

gem 'webdriver-user-agent'        # Gem for simulating mobile device on browser

gem 'java'

#gem 'rukuli'

#gem 'oclc_webdriver_support', git: "http://git.ent.oclc.org/scm/automation/oclcwebdriversupport.git", branch: 'master'